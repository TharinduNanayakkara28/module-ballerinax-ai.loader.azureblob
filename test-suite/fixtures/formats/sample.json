{
  "marker": "FORMAT_MARKER_JSON",
  "title": "Azure Blob Loader JSON fixture",
  "description": "The Azure Blob Storage data loader retrieves documents from one or more containers and returns them as text.",
  "tags": ["azure", "blob", "loader", "tika", "pdf"],
  "records": [
    {"id": 1, "note": "Plain-text formats are decoded directly from their bytes, while PDF documents are parsed with Apache Tika."},
    {"id": 2, "note": "Virtual folders are expressed as slashes in the blob name, and recursion can be enabled per configured source."},
    {"id": 3, "note": "An optional extension allowlist restricts which blobs in a folder listing are turned into documents."},
    {"id": 4, "note": "Unsupported binaries such as images and Microsoft Office documents are skipped during folder loads."},
    {"id": 5, "note": "When a blob is named explicitly, a deliberately unsupported type is reported as an error instead of skipped."},
    {"id": 6, "note": "Listing follows the NextMarker pagination cursor so that large containers are fully enumerated."},
    {"id": 7, "note": "Each returned document carries metadata: the file name, MIME type, size, and any available timestamps."},
    {"id": 8, "note": "This fixture exists to exercise the loader end to end against a real storage account."},
    {"id": 9, "note": "It contains several paragraphs so that extraction has meaningful content to return and assert on."},
    {"id": 10, "note": "Text extraction should preserve the readable words on every page of a multi-page document."}
  ]
}
