<?xml version="1.0" encoding="UTF-8"?>
<fixture marker="FORMAT_MARKER_XML">
  <title>Azure Blob Loader XML fixture</title>
  <items>
    <item id="1">Plain-text formats are decoded directly from their bytes, while PDF documents are parsed with Apache Tika.</item>
    <item id="2">Virtual folders are expressed as slashes in the blob name, and recursion can be enabled per configured source.</item>
    <item id="3">An optional extension allowlist restricts which blobs in a folder listing are turned into documents.</item>
    <item id="4">Unsupported binaries such as images and Microsoft Office documents are skipped during folder loads.</item>
    <item id="5">When a blob is named explicitly, a deliberately unsupported type is reported as an error instead of skipped.</item>
    <item id="6">Listing follows the NextMarker pagination cursor so that large containers are fully enumerated.</item>
    <item id="7">Each returned document carries metadata: the file name, MIME type, size, and any available timestamps.</item>
    <item id="8">This fixture exists to exercise the loader end to end against a real storage account.</item>
    <item id="9">It contains several paragraphs so that extraction has meaningful content to return and assert on.</item>
    <item id="10">Text extraction should preserve the readable words on every page of a multi-page document.</item>
    <item id="11">The loader treats a path with a trailing slash as a folder prefix rather than an exact blob name.</item>
    <item id="12">A path without an extension that matches no blob is resolved as a folder prefix automatically.</item>
  </items>
</fixture>
