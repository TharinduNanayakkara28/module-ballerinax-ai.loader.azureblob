[package]
org = "tharindu"
name = "loader_test_suite"
version = "0.1.0"
distribution = "2201.12.0"

# Consume the Azure Blob loader from the LOCAL repository
# (pushed via: `bal push --repository=local` from the ballerina/ dir).
[[dependency]]
org = "ballerinax"
name = "ai.azure.storage.blob"
version = "1.0.0"
repository = "local"
